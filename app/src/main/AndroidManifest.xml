<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToyFirstMobile"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".activity.category.AdminCategoryActivity" />
        <activity android:name=".activity.register.UserRegisterActivity" android:noHistory="true" />
        <activity android:name=".activity.toy.AdminToyStoreActivity" />
        <activity android:name=".activity.login.UserLoginActivity" />
        <activity android:name=".activity.dashboards.AdminDashboardActivity" />
        <activity android:name=".activity.dashboards.UserDashboardActivity" />
        <activity android:name=".activity.category.UserCategoryActivity" />
        <activity android:name=".activity.toy.AdminAddToyActivity" android:noHistory="true" />
        <activity android:name=".activity.toy.UserToyStoreActivity" />
        <activity android:name=".activity.toy.AdminUpdateToyActivity" />
        <activity
            android:name=".activity.cart.UserCartActivity"
            android:noHistory="true" />
        <activity android:name=".activity.order.UserPlaceOrderActivity" android:noHistory="true"/>
        <activity android:name=".activity.order.UserOrderConfirmationActivity" android:noHistory="true" />
    </application>

</manifest>